import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.brightenedmind.academy',
  appName: 'Brightened Mind Academy',
  webDir: 'out',
};

export default config;